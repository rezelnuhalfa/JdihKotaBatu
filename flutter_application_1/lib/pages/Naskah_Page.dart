import 'package:flutter/material.dart';

class NaskahPage extends StatelessWidget {
  const NaskahPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}